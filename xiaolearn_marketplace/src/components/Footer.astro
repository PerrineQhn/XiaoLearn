---
const currentYear = new Date().getFullYear();

const footerLinks = {
  produits: [
    { label: 'Application XiaoLearn', href: '/produits/abonnements' },
    { label: 'Dictionnaire Mobile', href: '/produits/abonnements' },
    { label: 'Manuels PDF', href: '/produits/manuels' },
    { label: 'Decks Anki', href: '/produits/flashcards' },
  ],
  ressources: [
    { label: 'FAQ', href: '/faq' },
    { label: 'À propos', href: '/a-propos' },
    { label: 'Contact', href: '/contact' },
  ],
  legal: [
    { label: 'Mentions légales', href: '/mentions-legales' },
    { label: 'CGV', href: '/cgv' },
    { label: 'Confidentialité', href: '/politique-confidentialite' },
  ],
};
---

<footer class="footer">
  <div class="footer-container">
    <div class="footer-brand">
      <a href="/" class="footer-logo">
        <img src="/logos/logo_asian-red.png" alt="XiaoLearn" />
      </a>
      <p class="footer-tagline">
        Apprenez le chinois mandarin de manière efficace et ludique avec nos outils basés sur le HSK 3.0.
      </p>
      <div class="footer-badges">
        <span class="trust-badge">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            <path d="M9 12l2 2 4-4"/>
          </svg>
          Paiement sécurisé
        </span>
        <span class="trust-badge">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"/>
            <path d="M12 6v6l4 2"/>
          </svg>
          Accès immédiat
        </span>
      </div>
    </div>

    <div class="footer-links">
      <div class="footer-column">
        <h4>Produits</h4>
        <ul>
          {footerLinks.produits.map(link => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>

      <div class="footer-column">
        <h4>Ressources</h4>
        <ul>
          {footerLinks.ressources.map(link => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>

      <div class="footer-column">
        <h4>Légal</h4>
        <ul>
          {footerLinks.legal.map(link => (
            <li><a href={link.href}>{link.label}</a></li>
          ))}
        </ul>
      </div>
    </div>
  </div>

  <div class="footer-bottom">
    <div class="footer-container">
      <p>&copy; {currentYear} XiaoLearn. Tous droits réservés.</p>
      <p class="footer-app-link">
        <a href="https://app.xiaolearn.com" target="_blank" rel="noopener">
          app.xiaolearn.com
        </a>
      </p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--bg-primary);
    border-top: 1px solid var(--border-light);
    margin-top: auto;
  }

  .footer-container {
    max-width: var(--content-max-width);
    margin: 0 auto;
    padding: var(--spacing-3xl) var(--spacing-lg);
    display: grid;
    grid-template-columns: 1.5fr repeat(3, 1fr);
    gap: var(--spacing-2xl);
  }

  .footer-brand {
    max-width: 320px;
  }

  .footer-logo img {
    height: 45px;
    width: auto;
    margin-bottom: var(--spacing-md);
  }

  .footer-tagline {
    font-size: 0.9rem;
    color: var(--text-secondary);
    margin-bottom: var(--spacing-lg);
    line-height: 1.6;
  }

  .footer-badges {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-sm);
  }

  .trust-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-xs);
    padding: var(--spacing-xs) var(--spacing-sm);
    background: var(--jade-green-light);
    color: var(--jade-green);
    border-radius: var(--radius-full);
    font-size: 0.75rem;
    font-weight: 600;
  }

  .footer-column h4 {
    font-size: 0.9rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--spacing-md);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .footer-column ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
  }

  .footer-column a {
    font-size: 0.9rem;
    color: var(--text-secondary);
    transition: color var(--transition-fast);
  }

  .footer-column a:hover {
    color: var(--primary-red);
  }

  .footer-links {
    display: contents;
  }

  .footer-bottom {
    border-top: 1px solid var(--border-light);
  }

  .footer-bottom .footer-container {
    padding: var(--spacing-lg);
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .footer-bottom p {
    font-size: 0.85rem;
    color: var(--text-tertiary);
  }

  .footer-app-link a {
    color: var(--primary-red);
    font-weight: 500;
  }

  .footer-app-link a:hover {
    text-decoration: underline;
  }

  @media (max-width: 900px) {
    .footer-container {
      grid-template-columns: 1fr 1fr;
    }

    .footer-brand {
      grid-column: 1 / -1;
      max-width: none;
    }
  }

  @media (max-width: 640px) {
    .footer-container {
      grid-template-columns: 1fr;
    }

    .footer-bottom .footer-container {
      flex-direction: column;
      gap: var(--spacing-sm);
      text-align: center;
    }
  }
</style>
